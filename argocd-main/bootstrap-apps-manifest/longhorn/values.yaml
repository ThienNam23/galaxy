longhorn:
  defaultSettings:
    nodeDownPodDeletionPolicy: do-nothing
  ingress:
    enabled: true
    ingressClassName: nginx
    host: longhorn.thiennam23.dev

serviceMonitor:
  enabled: true
  endpoint:
    path: /metrics
    port: manager
    interval: 30s
